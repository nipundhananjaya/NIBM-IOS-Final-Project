# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NIBM Connect' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NIBM Connect
    
    pod 'Firebase/Core'
    pod 'Firebase/Auth'
    pod 'Kingfisher'
    pod 'Firebase/Database'
    
end
